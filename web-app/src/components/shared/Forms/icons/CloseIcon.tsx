import close from '../../../../assets/svgs/close.svg';

interface Props {}

const CloseIcon = (props: Props) => {
  return <img src={close} alt='Close icon' width='20' height='20' />;
};

export default CloseIcon;
